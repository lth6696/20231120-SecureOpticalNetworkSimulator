package simulators.SurvivableRouting;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class EveasdroppingRisk {

    private static final Logger logger = LogManager.getLogger(EveasdroppingRisk.class);
    public EveasdroppingRisk(PhysicalTopology pt){
        logger.trace("3.1.risk");
    }
}
